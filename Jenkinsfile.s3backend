pipeline {
    agent any

    stages {
        stage('Configure') {
            steps {
              sh '''
                cd tf_s3_backend
                terraform init
                terraform plan
                terraform apply -auto-approve'''
            }
        }
    }
}
